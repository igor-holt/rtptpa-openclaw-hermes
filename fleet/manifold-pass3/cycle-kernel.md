# Pass 3 Evolved — Cycle Kernel

One task in. Seven beats. One vector out. Stop.

## Input

```
{"task":"...","pass":3,"risk_R":0.0,"surfaces":[],"elapsed_cycles":0}
```

## Beats

1. orchestrator → `{height, binding_constraint, next_speaker, forbidden_moves}`
2. accumulator → compounding test on the task
3. catalyst → one intervention or null
4. bound-watcher → surplus inquisition on that intervention
5. decoherence-arbiter → `{crystallize, reinvest, leave_uncollapsed}`
6. invariance-engine → `{judgment, next_vector}`
7. scribe → three statements only

## Stop

Emit `CYCLE` record. Do not start beat 1 again unless the operator supplies a new task.

## Forbidden

New catalog name. New Worker. Second source of truth. Asking all seven roles to speak in parallel.
